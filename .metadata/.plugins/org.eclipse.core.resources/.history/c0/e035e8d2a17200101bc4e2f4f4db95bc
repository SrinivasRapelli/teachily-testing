package com.teachily.pages;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.Select;
import org.openqa.selenium.support.ui.WebDriverWait;

public class ResourcesPage {
	
	WebDriver driver;
	
	public ResourcesPage(WebDriver driver) {
		this.driver = driver;
	}
	
	By resourcesLink = By.xpath("//*[text()='Resources']");
	By subject = By.xpath("//*[@id = \"subject-select\"]");
	By ks = By.xpath("//*[@id=\"keyStage-select\"]");
	By curriculum = By.xpath("//*[@id='curriculum-select']");
	By goButton = By.xpath("//*[text()='Go']");
	By glaciation = By.xpath("//*[text()='Glaciation']");
	By valley = By.xpath("//*[text()=\"Valley features\"]");
	
	
	public void clickOnResourcesLink() throws InterruptedException {
		WebDriverWait wait = new WebDriverWait(driver,Duration.ofSeconds(30));
		WebElement element = wait.until(ExpectedConditions.visibilityOfElementLocated(resourcesLink));
		element.click();
		Thread.sleep(4000);
	}
	
	public void selectSubject() throws InterruptedException {
		WebDriverWait wait = new WebDriverWait(driver,Duration.ofSeconds(30));
		WebElement element = wait.until(ExpectedConditions.visibilityOfElementLocated(subject));
		Select select = new Select(element);
		select.selectByVisibleText("Geography");
		Thread.sleep(2000);
	}
	
	public void selectKeyStage() throws InterruptedException {
		WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(30));
		WebElement element = wait.until(ExpectedConditions.visibilityOfElementLocated(ks));
		Select select = new Select(element);
		select.selectByVisibleText("Key Stage 4");
		Thread.sleep(2000);
	}
	
	public void selectCurriculum() throws InterruptedException {
		WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(30));
		WebElement element = wait.until(ExpectedConditions.visibilityOfElementLocated(curriculum));
		Select select = new Select(element);
		select.selectByVisibleText("England");
		Thread.sleep(2000);
	}

	public void clickOnGoButton() throws InterruptedException {
		WebDriverWait wait = new WebDriverWait(driver,Duration.ofSeconds(30));
		WebElement element = wait.until(ExpectedConditions.visibilityOfElementLocated(goButton));
		element.click();
		Thread.sleep(3000);
	}
	
	public void selectAResource() {
		WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(30));
		WebElement element = wait.until(ExpectedConditions.visibilityOfElementLocated(glaciation));
		element.click();
	}
	
	public void openAResource() {
		WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(30));
		WebElement element = wait.until(ExpectedConditions.visibilityOfElementLocated(valley));
		element.click();
	}
	
	public void readTheDescriptionOfResource() {
		WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(30));
		WebElement element = wait.until(ExpectedConditions.visibilityOfElementLocated(valley));
		System.out.println(element.getText());
	}
	
	
}
